# R0.74R formal figure package

Regenerate with the bundled Python runtime:

```bash
python plot.py
python validate.py
```

The package is deterministic and analytic. It contains no simulation or DNS.
The public SVG/PDF/PNG are mirrors of `figure.*`; `manifest.json` and
`SHA256SUMS` bind the package and frozen research inputs.
