# Figure R0.52-1

**The complete affine charge-weight family has a globally certified
width-\(10^{-40}\) optimum enclosure.**  (a) Exact-rational presentation
samples of the eliminated global feasibility margin
`-c^2 E(r_U,c)` on both sides of the second positive derivative root
`c_2`, plotted against `|c-c_2|`.  The curve remains positive down to the
root-box scale.  The dashed line marks the conservative lower margin obtained
from the largest (least negative) of all 317 Bernstein coefficients on the
complete `c_2` box.  The sampled profile illustrates the local geometry; the
global sign theorem uses three exact sign-changing derivative-root boxes,
Descartes' exact three-root upper bound, endpoint monotonicity, and the
complete Bernstein coefficient list.  (b) Each rational root-box width is
`1e-40`, while the exact Krawczyk image radii are of order `1e-79`.  Strict
interior inclusion proves one and only one simultaneous active/zero stationary
root in the three-dimensional box.  Positive KKT multipliers and an exact
second-order bound separately prove strict constrained local maximality.
(c) All 242 inactive finite-charge and all-order sector envelopes on the root
box.  The two active equalities are `(j,s)=(81,162)` and `s=0`; the nearest
inactive sector is `(j,s)=(82,164)`, with gap greater than
`1.4527650997576197e-4`.

The globally optimized affine-family radius satisfies

`0.3826244718485988314760952288871012330925 < R_aff <`
`0.3826244718485988314760952288871012330926`.

All formal signs, root counts, interval inclusions, sector envelopes, and
Bernstein coefficients use GMP rational arithmetic.  The result concerns the
complete `c>0, lambda>=0` affine family for one finite exact degree-80 center
of the reduced canonical edge generating system.  The interval does not by
itself identify an exact-real unique global maximizer, provide a critical-space
bridge for arbitrary three-dimensional velocity fields, or prove or disprove
three-dimensional Navier--Stokes regularity.
