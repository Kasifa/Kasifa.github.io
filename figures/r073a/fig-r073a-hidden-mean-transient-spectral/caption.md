**R0.73A normalized hidden mean, explicit transient envelope, and finite frozen
projection audit.** (A) For initial `h(s)=0`, `r(s)=W_xx(s)`, the exact hidden
mean excitation is
`h_d/(i c_mu)=e^(-2d)/[8(1+mu)]+e^(-8d)/[8(4+mu)]`. This normalized bracket
converges as `mu -> 0` to
`Pi_0(W^2)=e^(-2d)/8+e^(-8d)/32`, whereas the abstract mean-zero tangent model
contains no hidden coordinate. A nonzero limit of `h_d` follows only along
`c_mu -> c0 != 0` (equivalently `Lambda_mu ~ 1/gamma`). At fixed `Lambda`,
`c_mu -> 0`, and the limit of `h_d` is not decided here. Thus the plotted
nonzero quantity is a path-sensitive normalized bracket, not an unconditional
physical/abstract mismatch or a numerical residual. For every positive gap the
instantaneous lifted line is not invariant, so this identity is not a closed
one-dimensional positive-gap model. (B) The exact kernel
`J(s,d)=7/4(e^-s-e^-d)+1/2(e^-4s-e^-4d)` and the proved envelope
`exp[-mu(d-s)+|c|J(s,d)]` are shown for declared parameters. No certified
`X_mu` propagator-gain grid exists at source freeze, so no measured-gain curve
is plotted; the reserved overlay remains visibly pending and formal sealing is
blocked. (C) At the declared finite truncation `N=40`, spectral edge and
numerical abscissa are compared across the unprojected matrix, deletion of the
instantaneous `W_xx` direction, and deletion of
`span{sin x,sin 2x}`. Fixed projection is not uniformly stabilizing, and a
nonpositive spectral edge can coexist with positive numerical abscissa. Panel
C is finite Fourier--Galerkin screening only: there is no Galerkin tail bound,
infinite-dimensional spectral theorem, continuous-time maximum-gain theorem,
or nonautonomous concatenation result. Low-gap physical kinetic control,
Bloch-uniform direct sum, nonlinear Navier--Stokes closure, and the Clay problem
remain open.
