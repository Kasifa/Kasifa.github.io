# R0.74H collar-flux repair figure package

Deterministic two-panel journal figure for the R0.74H collar-flux repair.
Panel A records the exact energy-ledger and two-regime implications. Panel B
compares only certified powers of (L), after normalization by (B^2R^2).

Run the commands in `command.txt`. The producer must reproduce the frozen
25/25 certificate byte for byte before rendering. The independent validator
checks provenance, formulas, file inventory, 600-dpi output, embedded PDF
fonts, SVG palette, layout bounds, grayscale/final-size/PDF QA, and scope
labels. This package is an exact exponent diagram, not DNS, simulation, or a
Clay result.
