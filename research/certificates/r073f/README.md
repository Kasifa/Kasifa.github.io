# R0.73F certificate package

This package binds the R0.73F operator theorem to the immutable analytic source
commit `5edb1702314feca3e9d47a186b30fc53079cd67a`, recomputes the rational
roughness sentinels independently, verifies the finite diagnostic and figure
ledgers, and records the exact claim boundary.

The certificate is a deterministic consistency and provenance certificate.  It
does not machine-prove the prose operator argument.  The independent analytic
audit at the bound source commit supplies the theorem-level review.  The
R0.73F implication is conditional on the R0.73C eigenvalue, the R0.73E uniform
frozen dichotomy, and the R0.73B upper bound exactly as stated in the proof.

The finite Fourier package and journal figure are content-addressed but not yet
Git-sealed.  They are diagnostics only: `d_diag=0.01` is not the existential
analytic `d0`, cutoff agreement is not a continuum tail bound, and sampled
propagation is not a continuum evolution proof.  R0.73F concerns one exact
linear invariant row.  It proves no all-row closure, nonlinear mode-convolution
estimate, singularity, global-regularity failure, or Clay Millennium result.

Run the three commands in `command.txt` from the repository root.  The final
ledger covers every regular file in this directory except `SHA256SUMS` itself;
`manifest.files` excludes the self-referential manifest and ledger and states
that policy explicitly.
