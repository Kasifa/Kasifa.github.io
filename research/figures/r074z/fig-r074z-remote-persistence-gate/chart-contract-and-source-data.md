# Chart contract and source-data ledger

## Analytical question

When does persistence of the outer packet's adjacent-inward kinetic witness
force its own exterior cubic payment, and which cancellation-cell branches
remain unclassified?

## Supported statements

1. Finite inversion-paired passive correctors re-evolved under the same odd
   shear remain an exact smooth periodic unforced Navier--Stokes family.
2. The adjacent-inward clock weight is \(\Gamma^{1/4}\); the same physical
   shell in the doubled-radius payment has weight \(\Gamma^{1/16}\).
3. A persistent total-field kinetic floor satisfies the deterministic Hölder
   coercivity (Z.15)--(Z.16).
4. The exact rate gap is positive only on the strict side
   \(\limsup\kappa_L<\kappa_*\). The equality/critical layer is open.
5. Endpoint-to-\(R^3\)-tube persistence is conditional on the moving-frame
   envelope (Z.22), endpoint preservation, and a uniform moving-strip
   all-winding comparison.
6. The complexity coefficient in Panel C is a necessary leading rate within
   that derivative/conditioning model. It is neither sufficient nor an
   equality-case theorem.
7. The coercivity compares payment with a kinetic floor \(h\); it does not
   upper-bound the completed clock \(K\). Accumulated rows and full-clock
   Y.57 remain open.

The figure must reject any statement that the critical layer is closed, that
(Z.22) alone is an unconditional persistence theorem, that the W-kinetic
no-go proves the full-clock Y.57 theorem, that accumulated clock rows are
controlled, or that the bounded literature search proves novelty.

## Source-data semantics

`source-data.csv` is generated deterministically by `plot.py` with columns
`panel`, `series`, `x`, `y`, `x_unit`, `y_unit`, `exact_value`, `role`,
`source_locator`, and `note`.

- Panel A rows encode exact shell/weight identities, not measurements.
- Panel B contains an exact rational sample of the affine lower-rate function
  \(\Delta_{\rm rem}-2\kappa/3\); its zero is exactly \(\kappa_*\).
- Panel C rows encode the conditional proof map and exact additive
  decomposition of the necessary complexity rate.
- Panel D rows are logical statuses, not a quantitative ranking.

## Encoding

- one navy root plus neutral greys;
- exact/proved elements use solid strokes, conditional implications use
  dashed strokes, and open boundaries use open marks or hatch;
- no conclusion depends on colour alone;
- the research blossom remains locked at the top-right;
- the required scope label appears verbatim in every final export.

## Bound inputs

- main: `research/r074z_cancellation_cell_gate.md`, SHA-256
  `bb766da4002da760c35185294081f80df97c349ea08b198a5f76db31663aaf6a`;
- primary audit: `research/r074z_cancellation_cell_gate_primary_audit.md`,
  SHA-256 `6b867551bce840cb382cd13cb2ff298affbf0c0d8b1357a8163c5cedc9bace08`,
  PASS with blocker count 0;
- literature audit: `research/r074z_cancellation_cell_gate_literature_audit.md`,
  SHA-256 `8e5346ecf3c2beef4a620e0844e790703b628388ca7f0a6997aae88818caa82f`,
  bounded primary-source non-hit only.

All bindings are live-file SHA-256 precommit bindings, not Git seals.
