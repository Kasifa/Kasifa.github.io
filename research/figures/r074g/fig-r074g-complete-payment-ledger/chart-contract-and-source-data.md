# Chart contract and source data

**Analytical question.** Do the frozen R0.74G constants leave strict room for every payment row, and what do the resulting normalized analytic formulas predict at the two certified discrete scales?

**One-sentence takeaway.** The 31/31 exact certificate leaves three strictly positive exponent margins; the analytic ledger has a unit background floor, exponentially vanishing packet envelopes, and a ratio lower bound growing like `L`, which is the scaling mechanism rejecting the frozen proposed inequality.

**Surface and family.** Static two-panel journal figure, 180 x 82 mm. Panel A is a proportional benchmark ladder with an enlarged close-range view and exact gap cards. Panel B is an exact-formula ledger table at `j = 12, 13`; it uses direct `log10` labels because the packet envelopes are far below floating-point plotting range. SVG/PDF/approximately-600-dpi PNG are generated by `plot.py` with ReportLab.

**Data sufficiency.** `source-data.csv` has 16 rows. Eight Panel-A rows are exact rational coefficients or gaps recovered from the frozen certificate. Eight Panel-B rows retain the full symbolic formula, exact `L_j`, transformation, and deterministic analytic evaluation. No fitted values, sampled paths, DNS, simulations, or empirical observations are present.

**Palette policy.** Hard two-root cap: blue and gold plus neutral ink/greys. Circle, diamond, square, triangle, open/filled states, solid/dashed strokes, separate ledger rows, and direct labels preserve meaning without hue.

**Output footprint.** Live-text SVG, one-page embedded-font PDF, approximately 600 dpi master PNG, grayscale derivative, final-size derivative, and PDF render. The research blossom is locked to the top-right header.

**Claim boundary.** Supported by the figure package: exact finite arithmetic, exact formula provenance, deterministic analytic evaluations, and the stated normalized scaling directions. The finite certificate alone does not establish the heat-kernel, Brownian-bridge, local-pressure, Peetre, complete-payment, or endpoint proof. “Proposed inequality rejected” refers only to the frozen inequality defined in the R0.74G research note. It is not a singularity or regularity theorem and not a Millennium-problem solution. **NOT CLAY.**

**QA surface.** `validate.py` executes 50-plus independent checks covering both frozen source hashes, byte-identical certificate regeneration, exact fractions, exact source rows and formulas, dimensions/formats, embedded PDF fonts, live SVG text, required claim-boundary phrases, limited palette, and recorded text-extent proxies. Human visual QA inspects the master PNG, grayscale derivative, PDF render, and final-size derivative.
