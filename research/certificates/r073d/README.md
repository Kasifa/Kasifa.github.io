# R0.73D formal certificate

This package seals the static vanishing-viscosity cluster theorem to exact
Git source blobs.  The primary path checks the full proof/report/audit and the
finite diagnostic boundary.  A stdlib-only independent path recomputes the
exact Fourier commutator and compact-term sentinels without importing the
primary generator.  The comparator requires both paths to pass.

The certificate closes only the fixed spectral cluster:

```text
staticVanishingViscosityPersistence=CLOSED
fixedClusterRieszProjectionNormConvergence=CLOSED
fixedClusterAlgebraicMultiplicityPreserved=CLOSED
```

The contour radius and viscosity threshold are existential; algebraic
simplicity, the whole right-half-plane complement, fast-time transfer,
nonlinear Navier--Stokes control, and Clay remain open.
