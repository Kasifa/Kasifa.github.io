**Figure R0.74I. Moving-tube criterion and logarithmic exponent screen.**
(A) Sufficiently small moving-tube energy confines the mollified path within
half a radius, embeds the fixed half-radius cylinder, controls its normalized
cubic velocity integral by the `E_R^(3/2)` scale, and reaches a regular point
through a velocity-only one-scale epsilon criterion. The path is schematic;
no path samples or unknown constants are plotted. (B) For a candidate bound
`Y <= K P^(2/3)(1+log P)^gamma`, the explicit packet family leaves the exact
residual `L`-power `y(gamma)=1-2 gamma`. Hence `gamma<1/2` is rejected,
`gamma=1/2` remains an open endpoint, and `gamma>1/2` is only not rejected,
not proved. The screen applies along the realized lacunary `P_j` sequence.
The figure is an exact diagram, not DNS, not simulation, and not a Clay result.
