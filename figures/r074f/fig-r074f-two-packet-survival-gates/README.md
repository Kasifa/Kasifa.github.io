# R0.74F two-packet survival finite-gates figure

This 24-file journal package visualizes only the exact finite compatibility checks in the frozen R0.74F 30/30 certificate: the conditional exponent hierarchy, the finite buffer threshold, the first admissible discrete scale, and the conditional terminal-lobe annular inclusion.

It is an analytic bookkeeping figure, not DNS or simulation. The certificate does not prove the periodic Brownian-bridge bound, heat-kernel copy estimates, Feynman--Kac time ordering, packet survival or sign, pressure/payment rows, amplitude closure, endpoint divergence, or regularity. **NOT CLAY.**

Run `plot.py`, then `validate.py`; see `command.txt`. The generator refuses any certificate whose SHA-256 differs from the frozen 30/30 PASS object. `source-data.csv`, all image files, QA derivatives, manifests, and validation outputs are generated and must not be hand-edited.
