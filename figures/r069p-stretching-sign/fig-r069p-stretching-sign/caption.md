**Figure R0.69P. Sharp vortex-stretching geometry does not lower the
energy exponent.** **a,** For the unit axisymmetric extensional strain
`diag(-1,-1,2)/sqrt(6)`, the stretching coefficient is
`(3 cos^2(theta)-1)/sqrt(6)`. It attains the universal upper endpoint
`sqrt(2/3)` when vorticity is aligned with the extensional eigenvector and
changes sign at `theta=acos(1/sqrt(3))`; the equality jet is realized on a
ball by the compactly supported solenoidal field
`v_A=curl(chi[-x cross (Ax)/3])`. **b,** After the global Betchov reduction,
the exact ratio `(-4 det S)/(lambda_2 |S|^2)` equals
`2r(1+r)/(1+r+r^2)` for `r=lambda_3/lambda_2>=1`, approaching the sharp
supremum two without crossing it. **c,** Normalizing the energy-only Young
optimization gives `4x^(3/4)-3x`, with its maximum at the exact optimizer
`D_*=[3 sigma^(3/2)/(4 epsilon)]^4` and remainder
`27 epsilon^(-3) sigma^6/256`, a sextic remainder. The plots are exact-formula visualizations
backed by the source-locked R0.69P certificate; they do not establish an
unconditional spacetime depletion estimate or Navier--Stokes regularity.
