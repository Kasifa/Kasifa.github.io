**R0.72Z signed Orr--Sommerfeld threshold, exact negative boundary, and
orientation-paid Squire transfer.** (A) The coarse signed relative-form
envelope `Theta_bar=4 M3 (g alpha^2)^(-5/2)` for
`|c|=4 alpha^(-5)`, with the admissible region
`Theta_bar <= theta0 < 1`, and the exact two-mode sequence
`a_n mu^(5/2) -> (sqrt(2)/27) exp(-d)`. Together they show that the
sufficient and necessary high-mode power is `g ~ alpha^(-2)`; the coarse
sufficient constant is not claimed optimal. (B) For `beta=n=d=0`, the exact
derivative `0.5 d_d ||q||_2^2 = |c|/[8 mu^(3/2)(1+mu)^(3/2)]-1` is positive
at sufficiently small positive `mu`, ruling out all-gap prefactor-one
`L_q^2` contraction. The exact abstract tangent solution `q_*=W_xx` has a
block norm ratio tending to one as `alpha` decreases to zero; it is not a
physical `mu=0` velocity-row claim. (C) Kinetic normalization replaces the raw
angle ratio by `chi=R/sqrt(1+R^2+(rho/gamma)^2) <= 1`, but the sharp payment
`|Lambda|` remains. The displayed normalized history multipliers are
`chi min{g^(-1),A_vartheta alpha^2}` and
`chi min{(2g)^(-1/2),sqrt(B_vartheta) alpha}`, conditional on the declared
`Q` history. Every curve is a closed-form audit guide, not a PDE simulation or
analytic proof. Low-gap transient Orr--Sommerfeld control, the physical direct
sum, nonlinear closure, and the Clay Millennium problem remain open.
