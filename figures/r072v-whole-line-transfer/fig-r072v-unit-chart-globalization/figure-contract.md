# R0.72V figure contract

- The package identifier is fig-r072v-unit-chart-globalization.
- The final master is 178 mm wide and 82 mm high, with vector PDF and SVG plus
  a 600 dpi PNG.
- Panel A displays exactly
  \(\kappa(\theta)=(5/6292)\cos^2\theta+(1/44)\sin^2\theta\), its floor
  \(5/6292\), and the probe ledger
  \(q_0=(315/128)(1-4y^2)^4\mathbf1_{[-1/2,1/2]}\),
  \(\mu_2=1/44\), \(\mu_4=3/2288\).
- Panel B displays \(b=a^2/3+6c\) for \(c=-2,0,2\), with distinct line styles,
  direct labels, and exact markers at \(a=3k\), \(-4\le k\le4\).
- Panel C displays the implication chain
  disjoint cells -> H^-1 direct sum -> whole-line graph -> energy contraction.
  Its optional curve is exactly \(r=s/(1+s)\), \(s=C_T^2/T\), and is labelled
  formula-only. It must never be described as a numerical estimate of \(C_T\).
- The graphic visibly states
  whole-line block contraction: CLOSED (exact cubic energy model)
  and periodic / Clay: OPEN.
- The only chromatic roots are blue #285f8f and gold #a6781f. Ink, muted gray,
  grid gray, white, and pale gold are neutral roles.
- Every data distinction also uses stroke style, direct text, or markers.
- A five-petal blossom is locked to the top-right header. It is decorative and
  carries no data.
- pdeSimulation=false. Every plotted curve is an analytic presentation.
- The finite certificate does not machine-check compactness, the infinite
  direct sum, evolution construction, cutoff energy, or the analytic theorem.
- A formal build may copy byte-identical PDF/SVG/PNG masters only to
  public/assets/r072v, after formal certificate validation, a distinct
  certificate commit, a clean tree, absent output targets, and explicit visual
  inspection.
- The source stage contains exactly the eleven declared source files and no
  generated output, manifest, hash ledger, QA image, or public copy.
