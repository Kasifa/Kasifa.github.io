# Chart contract and source data

- Figure id: `fig-r074r-clock-triage`.
- Purpose: compactly separate the proved window theorem, the proved
  arbitrary-clock triage, and the still-open universal extraction input.
- Source data: `source-data.csv`, generated exactly from `config.json`.
- Mathematical bindings: R0.74R Steps 1–2 and their primary/independent
  certificates named in `manifest.json`.
- Forbidden inference: the abstract/functional no-go rows are not NSE
  counterexamples and the conditional arrow is not an unconditional Q.1
  theorem.
- Rendering: one-page 178×100 mm SVG/PDF plus 600 dpi PNG.
