# Visual QA report

Status: passed

The regenerated 178 mm figure was inspected at final size in color, true
grayscale, and after rendering the vector PDF at 180 dpi. Panel titles, axis
labels, legends, signed values, log scales, annotations, arrows, and the scope
line are legible and unclipped. The hatched commutator bar, markers, labels,
and line styles preserve the distinctions without color.

The evidence wording keeps Panels A-B as one deterministic finite Fourier
diagnostic, Panel C as exact heat-flow scaling rather than an NSE solution
counterexample, and Panel D as a boundary of the displayed direct insertion
rather than a logical non-implication. The dashed arrow and cross visibly mark
the failed direct insertion; R0.71N is separately attached to whole-scalar
fusion. Plotted values were cross-checked against `data.csv`,
`validation.json`, and the independent archive after the final rerender.
