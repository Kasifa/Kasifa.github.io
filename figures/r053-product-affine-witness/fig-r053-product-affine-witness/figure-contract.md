# R0.53 figure contract

- Analytical question: does the two-factor charge weight
  `c^s(1+lambda|s|)(1+mu|s|)` strictly improve the complete single-affine
  family, which column becomes sharp, and are all other all-order records
  still separated?
- Takeaway: one simple rational symmetric product-affine witness has a unique
  zero-charge threshold root and strictly exceeds the complete R0.52 affine
  upper bound.  The fixed restart gains more than 9.2209 ppm and the isolated
  threshold gains more than 10.7949 ppm; all 281 inactive records stay strict.
- Family and variants: focused signed threshold profile; strict-gain
  lollipops; ranked logarithmic competitor-gap curve.
- Data sufficiency: 121 exact-rational presentation samples resolve the two
  leading columns around the threshold; two exact ratios compare R0.53 lower
  bounds with the R0.52 complete-family upper bound; every formal competitor
  record is retained.  Root isolation and dominance come from the pinned GMP
  certificate, not interpolation of the display samples.
- Renderer: reproducible Matplotlib static export.
- Palette policy: hard two-root cap using blue and gold plus neutral ink.
- Non-color encoding: solid and dashed lines, open circles, filled diamonds,
  a square tail marker, ordering, and logarithmic position preserve meaning in
  grayscale.
- Output footprint: 178 by 112 millimetres; PDF, SVG, and 600 dpi PNG.
- Final QA: color PNG, true grayscale conversion, and Poppler-rendered PDF at
  final size, followed by independent data and archive validation.
