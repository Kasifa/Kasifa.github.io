# R0.71T figure contract

- **Analytical question:** does finite Galerkin evolution corroborate the
  positive-time internal target-shell zero produced by the local NSE flow-map
  implicit-function construction, and does the associated two-parameter
  scaling separate the entry atom from the bare normalized Leray time budget?
- **Takeaway:** the shot target shell crosses zero at (t=	au>0), the
  precompensation has the predicted first-order coefficient, the entry atom
  agrees with the scale-zero slope charge, and the leading atom-to-bare-budget
  ratio grows like (lambda^2).
- **Surface:** static double-column journal figure, 178 mm by 124 mm.
- **Renderer:** reproducible Matplotlib PDF/SVG/600 dpi PNG export.
- **Panel A:** five logarithmically ordered time values show
  (|z_	au|/(	au|P_*F(u_*)|_2)	o1), with one independent refined
  direct-convolution point. A focused vertical scale is stated visibly.
- **Panel B:** 161 ordered time samples show the signed principal target-shell
  coefficient across (t/	au=1); a separate log axis exposes the transverse
  residual rather than hiding it on the signed scale.
- **Panel C:** five time values compare (A_+), the slope-charge
  reconstruction, and the exact seed limit (1/4), with one independent
  refined point.
- **Panel D:** seven integer dilations show exact leading (lambda^{-4}),
  (lambda^{-6}), and (lambda^2) laws. These are formula values, not fitted
  exponents.
- **Palette:** blue and ochre roots plus ink/gray neutrals; solid/dashed/dotted
  lines, filled/open markers, direct axis labels, and fixed panel order retain
  meaning without color.
- **Boundary:** panels A--C are finite Fourier--Galerkin corroboration in the
  (x_3)-independent three-component invariant sector. Panel D is an exact
  leading asymptotic ledger. There is no continuum Galerkin error estimate,
  DNS claim, Leray occupation bound, repeated-entry packing result,
  singularity result, continuation theorem, or regularity theorem.
