# Chart contract and source-data ledger

The figure explains why either persistence or endpoint rise forces local spacetime mass, and why this closes only the W-remote witness.

Supported: strict nested geometry; exhaustive two-case dichotomy; exact Hölder/weight/endpoint exponent algebra; positive rate without a persistence fraction; total-field coverage of finite cancellation/windings. Open: complete clock, fixed deletion, arbitrary suitable weak solutions, and regularity.

`source-data.csv` is deterministic analytic bookkeeping, not measurements. Solid paths are exact/proved; dashed or hatched cards are open. No conclusion depends on colour. Bindings and core commit are in `config.json`; the literature audit is not a novelty determination.
