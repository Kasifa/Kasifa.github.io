# R0.70M exact certificate

This directory locks the exact symbolic payload for the strain-propagator
holonomy and affine-rank gate.

The producer verifies four groups:

1. the zero-signed-integral noncommutative strain loop and its exact
   hyperbolic, non-normal monodromy;
2. the sharp \(\kappa_2(G)^2\) Euclidean and optimized-shape quotient loss;
3. the congruence-invariant affine-relative SPD shape speed;
4. the covariance rank defect and non-uniform isotropic regularization.

Run the command in command.txt from the repository root. The result must be
byte-identical to result.json before SHA verification.

The finite calculation does not computer-prove a realization of the
four-pulse loop along one unforced finite-energy periodic NSE trajectory, an
energy-controlled critical estimate, a weak-solution passage, regularity,
blow-up, or a Millennium-problem solution.
