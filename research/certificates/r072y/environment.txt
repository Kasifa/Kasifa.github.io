R0.72Y deterministic certificate draft stage.
Standard-library Python only: argparse, fractions, hashlib, json, pathlib, re, subprocess, sys, typing.
Exact rational, integer-polynomial, Fourier-mode, and discrete symbolic-table arithmetic only.
No random numbers, floating-point fitting, PDE solve, GPU, network access, or external data.
Draft outputs are deterministic but intentionally lack a formal Git source-commit binding.
Formal mode requires one completely clean current HEAD, a full lowercase 40-character commit, and byte-identical Git blobs for the complete frozen source inventory.
Formal validation re-resolves the Git object, ancestry, source blobs, working hashes, SHA-256 values, and byte counts; 29 bound sources may not drift. The release-manifest source blob remains permanently checked, while its current copy may advance only in a completely clean descendant publication commit. At source HEAD only the five sealing outputs may be dirty.
Functional analysis, infinite-series convergence, Galerkin limits, endpoint traces, sharpness proofs, evolution existence, nonlinear closure, and Clay are not machine checked.
