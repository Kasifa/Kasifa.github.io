# R0.71A exact-certificate bundle

This directory archives the finite exact audit for the R0.71A
projector-coherence method boundary.

## Decision locked by the bundle

The producer checks two groups of facts.

First, a ten-mode periodic sign pair has

\[
 P_1=e_3\otimes e_3,
 \qquad
 \nabla P_1=0,
 \qquad
 [T_\alpha,I-P_1]=0,
\]

the same pointwise covariance for both signs, the gap bounds

\[
 \lambda_1-\lambda_2\ge10\Lambda^2,
 \qquad
 \frac{\lambda_1-\lambda_2}{\lambda_1}\ge\frac23,
 \qquad
 \frac{\lambda_1-\lambda_2}{\operatorname{tr}Q}\ge\frac12,
\]

and opposite covariance work

\[
 \mathfrak P_Q(\omega_{\Lambda,\pm})
 =\pm\frac{3\sqrt2}{40}\Lambda^3.
\]

Second, the exact exponent ledger verifies that the critical mixed-norm
projector estimate reaches \(L_t^1\), while an amplitude-normalized
concentrating sequence fixes the two energy norms and the critical projector
norm but makes the projector error grow in every \(L_t^s\), \(s>1\).

## Files

- result.json — canonical sorted JSON emitted by the producer;
- command.txt — exact reproduction command;
- environment.txt — pinned runtime and dependency record;
- SHA256SUMS — hashes for every archived payload and producer dependency;
- ../../r071a_exact_audit.py — R0.71A producer;
- ../../r070z_exact_audit.py — imported exact helper and prior ledger;
- ../../r070x_exact_audit.py — transitive Fourier dependency;
- ../../r070y_exact_audit.py — reused exact filler reference; and
- ../../r070y_report-source.md — analytic source for the zero-set lemma.

## Analytic boundary

The finite producer does not replace the Gagliardo--Nirenberg inequalities,
the compact cutoff argument, the infinite-dimensional middle-strain
continuation theorem, or the primary-source literature audit.

It proves a kinematic route obstruction, not failure of a Navier--Stokes
continuation criterion.  It proves no singularity, global regularity, or
solution of the Millennium problem.

No DNS, stochastic search, GPU, or DGX resource is used.
