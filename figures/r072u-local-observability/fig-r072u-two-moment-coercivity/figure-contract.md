# R0.72U figure contract

- The package identifier and directory stem are
  `fig-r072u-two-moment-coercivity`.
- The final journal master is 178 mm wide and 76 mm high, with vector PDF and
  SVG plus a 600 dpi PNG.
- Panel A uses exactly
  \(\rho=(315/256)(1-X^2)^4\mathbf 1_{[-1,1]}\), plots \(\rho\) and
  \(X\rho\), and displays \(\mu_2=1/11\), \(\mu_4=3/143\).
- Panel B uses exactly \(K_c(s)=3/143+6(c+s)/11\), \(|s|\le1\), and the
  three centres \(c=-27/13,0,27/13\). It displays the sufficient symmetric
  fixed-sign threshold \(|c|\ge27/13\).
- Panel C uses exactly \(3z^2+4/5\), \(z=X^2+2c\), and displays the separate
  unit-block fixed-gauge inviscid floor \(4/5\).
- The graphic visibly states `whole-line block contraction: OPEN`.
- The chromatic palette has a hard two-root cap: blue `#285f8f` and gold
  `#a6781f`, plus ink, muted, grid, paper, and a pale gold surface tint. Primary
  curves use blue; comparators, the floor, and the OPEN callout use gold.
- Colour never carries a distinction by itself: series also differ through
  solid/dashed/dotted strokes, direct labels, or threshold markers.
- A subtle five-petal research blossom is locked to the unused top-right header
  corner. It is decorative, uses only the two chromatic roots plus ink, and
  carries no data encoding.
- The archive is an exact-algebra presentation. It performs no PDE simulation,
  regression, fitted threshold search, or random sampling.
- The fixed-gauge floor is not described as a viscous contraction estimate.
- A formal build may copy byte-identical PDF/SVG/PNG files only to
  `public/assets/r072u`, and only after source-bound certificate validation and
  explicit final-size visual inspection.
- The source stage contains no generated data, figure, manifest, QA image,
  public copy, or hash ledger.
