# Figure R0.74A-1 | Localized K_D payments and function-level obstruction ledgers

**Panel A.** Under the normalization
`A_c=B_c=U_ext=D_ext=1`, the core--core and exterior--core rows in
Theorem 4.1 carry the exact scale weight `theta^(1/4)`, whereas the
core--exterior and exterior--exterior rows carry `theta`. The theorem's
unknown positive constant `C` is suppressed and is not estimated or plotted.

**Panel B.** For the exterior high-frequency packet with
`epsilon_N=N^(-2/3)` and `N=2^j`, the proved lower-bound factor is
`epsilon_N^3 N^2=N^0`; the unknown positive constant `c` is suppressed.
The old cubic payment has exponent `epsilon_N^3=N^(-2)`, while the packet's
gradient energy has exponent `epsilon_N^2 N^2=N^(2/3)`.

**Panel C.** For a time spike of length `delta` and amplitude
`delta^(-1/3)`, the integrated old cubic payment has exponent
`delta * delta^(-1)=delta^0`, while the essential-time exterior velocity
energy has exponent `delta^(-2/3)`. Each positive `delta` denotes a separate
finite energy-class field. The family has no uniform global
`L_t^infinity L_x^2` bound.

Panels B and C concern function-level packets, not unforced Navier--Stokes
trajectories. All rows are deterministic evaluations of normalized analytic
consequences: no simulation or DNS is performed, and finite rows do not prove
the analytic quantifiers. The figure is not an NSE counterexample and proves
no compactness, epsilon regularity, smoothness, or global regularity.
**NOT CLAY.**
