**Figure R0.72J-1. Mixed-parity cubic audit separates the exact graph obstruction from its finite physical payment.**
**A**, after reduction by the carrier gcd, the Cayley graph is bipartite exactly when every reduced carrier is odd.  Thus `{2,6}` reduces to `{1,3}` and retains the two-color split.  The set `{1,4}` is non-bipartite through a five-cycle but has no triangle.  The consecutive block `S_R={R,...,3R-1}` is triangle-rich. **B**, in `S_R` the signed ordered triple count is exactly `T_R=3R(R+1)`, and the uncorrected aligned coefficient satisfies `|P_0V^2F(0)|=T_R/sqrt(2)`. **C**, producer and independent binary64 routes agree on the true raw cubic `delta integral |hP_0V^2F|`; the last three dyadic sizes have log slope `1.950`, consistent with an order-`R^2` term. **D**, after the declared physical lift, the normalized true cubic decreases with last-three-point slope `-0.737`; `R^-2/3` is a displayed family-specific scaling guide.  The universal analytic envelope `R^-4/9(1+log R)^-2/3` is included only as a theorem-rate reference, not as a fit to these points. Filled circles denote the producer and open squares the independent route.  The finite launch constructs one exact complex root.  It does not enumerate complete roots, the real Rolle complete-root corollary does not apply, and the calculation does not prove a general Navier--Stokes result.
