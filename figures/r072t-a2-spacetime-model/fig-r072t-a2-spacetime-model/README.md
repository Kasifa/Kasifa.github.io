# Figure R0.72T — exact A2 spacetime model ledger

This is a three-panel analytic figure. Panel A samples the exact heat profile
and its scaled limit. Panel B draws the three exact affine exponent equations.
Panel C draws the exactly minimized characteristic action for the linear
time-drift calibration. No fixed-torus cubic PDE is simulated.

Generate from the repository root after building the exact certificate:

```sh
python3 scripts/generate_r072t_figure.py
```
