# R0.73X certificate claim boundary

## Gaussian channel

The archived Gaussian certificate proves only the frozen functional rows:

- the explicit pointwise bound
  \(|\mathscr S_s|\le C_0s^{-1/2}P_{2s}(|u|^3)\), with
  \(C_0=2^{5/2}e^{-1/2}+2^{7/2}/\sqrt\pi<10\);
- the displayed Gaussian annular and scale-integral coefficients;
- Navier--Stokes dimensional bookkeeping;
- the concentration powers \(3,9/2,-3/2,1\) for an explicit smooth static
  divergence-free packet;
- the interpolation powers \(\nu^{-3/4}\) and \(R^2\); and
- a super-exponential comparison that makes the lifted Gaussian tail
  summable.

The remote-packet witness quantifies over unconstrained smooth static
divergence-free velocities, or velocity-only functional inequalities for
which setting \(p=\mu=0\) is admissible.  The packet is not asserted to be an
unforced Navier--Stokes trajectory, and \(p=0\) is generally not its
associated pressure.  It therefore does not refute an associated-pressure
inequality or an NSE-trajectory-only inequality.

## Fourier channel

The exact channel proves the two-pair split

\[
 225/1024=27/512+171/1024
\]

and the exact fixed-harmonic-probe ratios
\(5|A|/304\) and \(|A|/80\).  These grow linearly in amplitude, so no
amplitude-independent quadratic absorption constant works in that declared
probe family.

The probe is positive and periodic but not compactly supported.  Thus the
compact-cutoff absorption candidate remains open.  The absolute heat-scale
slices are deterministic converged float64 diagnostics, not interval bounds,
DNS, or time-integrated tent/Carleson estimates.

## Not established

This archive proves no signed-to-absolute coercivity, exterior-tail smallness,
compact-cutoff absorption, weighted tent/Carleson estimate, suitable-weak
zero-scale endpoint, epsilon-regularity theorem, blow-up exclusion, arbitrary-
data three-dimensional global regularity, or Clay conclusion.  `NOT CLAY`.
