Run from the repository root with the research virtual environment.

PY=/Users/kasifa/Documents/Math/.codex-research-venv/bin/python
DIR=figures/r071v-level-boundary/fig-r071v-zero-level-boundary
export PATH=/Users/kasifa/.codex/dependencies/pdf/bin:$PATH

$PY $DIR/produce_data.py --config $DIR/config.json
$PY $DIR/plot.py --data $DIR/data.csv --results $DIR/results.json --config $DIR/config.json --output-stem $DIR/figure
$PY $DIR/qa_images.py --config $DIR/config.json --manual-status pending
$PY $DIR/validate.py --config $DIR/config.json --output $DIR/validation.json

After visually inspecting qa-original.png, qa-grayscale.png, and qa-pdf.png:

$PY $DIR/qa_images.py --config $DIR/config.json --manual-status pass
$PY $DIR/validate.py --config $DIR/config.json --output $DIR/validation.json
$PY $DIR/build_manifest.py

The PDF artifact-operation marker is coordinated once at the full R0.71V
release level and must not be rerun from this package.
