# Figure contract — R0.64 exact supercritical cycle

- **Question:** Does the zero-time (48)-state digit transfer admit a
  common norm with per-level factor at most (2)?
- **Exact result:** the word `0100` has a four-level product with a real
  eigenvalue in `(25,26)`, above the four-level threshold `16`.
- **Reachability:** the targets `q_r = 2(16^r-1)/15` observe the same
  dominant root through an exact scalar recurrence.
- **Boundary:** the figure does not decide the Gaussian-weighted simplex
  integral or the Navier--Stokes regularity problem.
- **Layout:** double-column, `178 mm × 96 mm`; state/cycle diagram at left,
  exact spectrum and reachable growth diagnostics at right.
- **Encoding:** blue for exact state reduction, rust for the supercritical
  root, black dashed threshold, open markers and direct labels so the claim
  remains legible in grayscale.

