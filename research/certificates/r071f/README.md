# R0.71F exact-certificate bundle

This directory archives the exact producer and independent checker for the
R0.71F localized projected-Lamb trace gate.

## Decision locked by the bundle

1. For a moving cutoff \(\chi_Q\), the projected-Lamb heat equation has an
   exact local ledger with time faces, both heat faces, cutoff--curl work,
   transport/shape residual, and viscous collar.
2. A matched bounded-overlap partition yields a valid local continuation
   consumer and the unconditional heat-height packing

   \[
   \sum_{j,Q}q_{j,Q}(s)\le N\|e^{s\Delta}L\|_2^2.
   \]

3. The spectral moment inequality has the exact constant
   \(N\Gamma(\alpha)/2^\alpha\) for every \(\alpha>0\).  Leray energy closes
   \(\alpha=1\), not every \(0<\alpha<1\).
4. For the fixed R0.71E low-radius frame values and the global-smooth 2D3C
   NSE solution generated by the six-mode datum, every nonzero smooth
   cutoff \(\phi\ge0\) gives strictly positive local work and

   \[
   q_\phi(s)=e^{-2K^2s}q_\phi(0).
   \]

5. Hence

   \[
   q_\phi(0)=\frac{2K^2}{1-e^{-2K^2h}}
   \int_0^h q_\phi(s)\,ds.
   \]

   The critical \(K^2\) trace cost is saturated, not disproved.
6. A matched partition preserves a two-sided low-block quotient bound.  With
   \(a=K^{-1}\), the full datum has fixed kinetic energy while the normalized
   low-block bottom stays positive and its heat bulk is \(O(K^{-2})\).
7. A separate one-block whole-space scaling argument rejects only pure
   geometry-only factors \(o(r^{-2})\); it does not reject a critical
   \(Cr^{-2}\) estimate, another prescribed frame, or estimates with
   additional terms.

These statements prove no singularity, unconditional regularity theorem,
novelty theorem, or Millennium-problem claim.

## Files

- result.json - canonical sorted JSON emitted by the exact producer;
- independent-result.json - independent FFT, cutoff--curl, and quadrature
  checker;
- command.txt - exact reproduction commands;
- environment.txt - pinned runtime and dependency record;
- SHA256SUMS - hashes for every archived payload and source dependency;
- ../../r071f_exact_audit.py - exact producer;
- ../../r071f_independent_audit.py - independent checker;
- ../../r071f_report-source.md - analytic report;
- ../../r071f_literature_audit.md - primary-source claim ledger;
- ../../r071f_independent_audit.md - independent mathematical audit.

## Reproduction boundary

The programs certify the finite Fourier reconstruction, localized
cutoff--curl identity, exact heat decay and trace multiplier, Gamma spectral
constant, matched-partition algebra, and fixed-energy normalization.  The
moving-cylinder ledger, general continuation implication, Leray-level
packing, and interior scaling theorem are proved analytically in the report.

No DNS, stochastic search, GPU, network, or DGX resource is required.
