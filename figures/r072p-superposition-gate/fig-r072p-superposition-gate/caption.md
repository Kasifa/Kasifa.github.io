**Figure R0.72P-1. A two-carrier full-superposition gate with a certified Morse cone.**
**A**, on one affine residue row each pair of signed `R` and `2R` shifts
contributes `2 cos(m phi)`, and the declared `epsilon` absorbs this factor;
`B=2` does not cancel it. The carriers reduce exactly to
`W(y,phi)=exp(-y)cos(phi)+lambda exp(-4y)cos(2phi)`; the displayed profiles
are formula slices, not a numerical evolution. **B**, for
`|lambda|<=1/8`, the factor `1+4lambda exp(-3y)cos(phi)` remains in
`[1/2,3/2]`. Therefore `W_phi` vanishes only at the fixed points `0` and
`pi`, with uniform shape constants. **C**, the exact time-slice loss of this
certificate occurs on `|lambda|=exp(3y)/4`. The blue safe cone stays a fixed
distance from the wall; the wall itself does not disprove enhanced
dissipation. **D**, after the Coble--He semigroup estimate and the compact
epsilon contraction completion, the R0.72O full-superposition implication
becomes proved in the declared class with physical ledger
`epsilon <= C p^(4/3) R^(4/3) L_(R,epsilon)^2`. The plotted curve is only the
fixed-polynomial-coupling asymptotic representative
`L_(R,epsilon) asymp 1+log R`, at `p=1/sqrt(2)`; it is not an exact window at
every epsilon. Open ochre markers retain the former conditional status;
filled blue marks record the R0.72P theorem at the same representative
values. Absolute theorem and comparability constants are suppressed. The
figure proves neither arbitrary carrier superposition nor general
three-dimensional Navier--Stokes regularity.
