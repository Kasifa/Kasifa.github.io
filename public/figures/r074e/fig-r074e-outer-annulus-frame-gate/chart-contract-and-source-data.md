# Chart contract and source data

**Analytical question.** Do the frozen R0.74E rational parameters simultaneously leave strict exponent room and place the paired target in an outer-annulus geometry where inversion oddness cancels the local frame at the origin?

**One-sentence takeaway.** Yes at the finite gate: `4/1323 < 1/320 < 49/14625`, `75/22528 > 1/320 > 8/3969`, and the exact odd/even symmetry gives `X_R = 0`, `a_R = a'_R = 0`; this does not establish packet survival or close the analytic ledger.

**Surface and family.** Static two-panel journal figure, 180 × 82 mm. Panel A is a benchmark/interval view with exact direct labels. Panel B is a normalized geometric schematic with an exact implication chain. The surface is SVG/PDF/PNG generated by `plot.py` using ReportLab.

**Data sufficiency.** `source-data.csv` contains 23 exact records: 13 exponent/parameter facts and 10 geometry/cancellation facts. Every numeric value is parsed from, or algebraically recomputed with `fractions.Fraction` from, `research/r074e_outer_annulus_exponent_certificate.json`. No fitted values, samples, simulations, or unknown constants are plotted.

**Palette policy.** Hard two-root cap: blue and gold plus neutral ink/greys. Bounds use open circles and dashed guides; the chosen exponent uses a diamond; leakage uses circle versus diamond versus square; inversion partners use filled versus open markers and a dashed connector. Meaning survives grayscale without relying on hue.

**Output footprint.** Live-text SVG, one-page embedded-font PDF, approximately 600 dpi master PNG, grayscale derivative, final-size derivative, and PDF render. A top-right research blossom is fixed in the header.

**Claim boundary.** Supported: finite exact rational compatibility, normalized annular geometry, exact odd/even cancellation, exact NSE/mean-zero/unforced status already proved in §9. Unsupported and visibly marked OPEN: two-packet survival, buffered leakage, transition/packet/pressure/copy rows, and amplitude closure. No endpoint-divergence, regularity, blow-up, or Millennium conclusion. **NOT CLAY.**

**QA surface.** `validate.py` checks certificate identity, exact fractions, source rows, dimensions/formats, embedded PDF fonts, live SVG text, required boundary phrases, and a recorded text-extent proxy. Human visual QA uses the master PNG, grayscale derivative, PDF render, and final-size derivative.
