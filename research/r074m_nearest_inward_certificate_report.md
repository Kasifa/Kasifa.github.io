# R0.74M finite certificate report — nearest inward expulsion

## Result

The primary exact-arithmetic producer

    scripts/r074m_nearest_inward_certificate.py

returns a machine-readable certificate with

    PASS 38/38.

The independent Ruby reconstruction

    scripts/r074m_nearest_inward_certificate_independent.rb

recomputes the same inventory from primitive constants using Ruby Rational
arithmetic and also returns

    PASS 38/38.

The independent implementation checks the complete seven-field content of
every row, row order and uniqueness, all derived fields, input and top-level
schemas, status flags, analytic boundary, summary, and the byte hash of the
certificate JSON.  It does not call the Python producer.

## Exact comparisons certified

### Final-segment geometry

\[
 \frac35-\frac{32}{63}-\frac1{16}
 =\frac{149}{5040}>0.
\]

Thus an inward endpoint plus an \(LR/16\) final Brownian modulus remains
inside the \(3LR/5\) caloric-defect window after the \(R/8\) padding is
absorbed.

### Heat-defect exponent

\[
\begin{aligned}
 \frac{L^2}{640}
 -\frac{16}{3903}\left(\frac35L+64\right)^2
 ={}&\frac{361}{4163200}L^2\\
 &-\frac{2048}{6505}L-\frac{65536}{3903}.
\end{aligned}
\]

At \(L=9216\), this margin and its derivative are

\[
 \frac{433872896}{97575}>0,
 \qquad
 \frac{41744}{32525}>0.
\]

The actual discrete family has \(L_{13}=16128\), above that threshold.

### Positive displacement

\[
 \frac{49}{14625}-\frac1{640}
 =\frac{3347}{1872000}>0,
\]

so the negative plateau error decays strictly faster than the retained heat
defect.  Also

\[
 \frac1{320}-\frac1{640}=\frac1{640}>0,
\]

which is the exact exponent reason

\[
 \Sigma_L=\frac1{32768}e^{-L^2/640}
 \gg LR=Le^{-L^2/320}.
\]

The finite threshold rows verify that the reserved displacement absorbs the
global negative term and is at least four times the padded inward radius.

### Fast-return paths

For a final interval \(R^2/64\) and modulus \(LR/16\), the reflection
exponent is exactly \(1/16\).  Its target reserve is

\[
 \frac1{16}-\frac1{320}-\frac2{1323}
 =\frac{24497}{423360}>0.
\]

This is the finite comparison which lets the exceptional path probability
pay one factor \(R\) and the annular weight ratio.

### Good-path tail and power ledger

\[
 2\left(\frac1{320}-\frac1{640}\right)=\frac1{320}>0,
\]

so \(\Sigma_L^2/R^2\) contains the super-Gaussian factor
\(e^{L^2/320}\).  Before exponential payments, the bad and good rows are
respectively

\[
 LR^4,\qquad LR^3.
\]

The bad-event reserve pays the missing \(R\), while the super-Gaussian
derivative-kernel tail pays \(R^2e^{-G_1L^2}\).

## Strict scope

The 38/38 result certifies only finite rational constants, monotone
thresholds, exact exponent margins, schemas, and the \(L,R\) power ledger.
It does not prove:

1. the normalized bridge or common-forward-law identity;
2. the Brownian reflection principle;
3. heat-kernel positivity or periodic Gaussian tails;
4. the support-conditioned displacement lemma;
5. synthesis of every shell row or the complete R0.74K signed condition;
6. a universal endpoint inequality, regularity, singularity, novelty, or
   priority.

Those statements are separated into the analytic proof, independent
analytic audit, and bounded literature record.  **NOT CLAY.**
