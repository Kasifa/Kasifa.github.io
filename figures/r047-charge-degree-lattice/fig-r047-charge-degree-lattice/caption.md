# Figure R0.47-1

**Exact charge--degree lattice endpoint certificate.** (a) For every fixed
positive charge `2<=s<241`, the exact degree factor is retained inside the
complete convex endpoint sum.  The 239 plotted values are therefore 239
all-degree theorems, not a tail-degree scan.  Their maximum at the target
radius `r=0.376932` is the true endpoint `s=162,j=81`, with value
0.9999973490826196656.  (b) For `s>=241`, the even and odd minimum-degree
lattice branches are exact rational functions of `y=1/s`.  All 319 exact
Bernstein coefficients of the even derivative on `[0,1/242]` are positive,
and all 319 coefficients of the negative odd derivative on `[0,1/241]` are
positive.  The plotted points sample those explicit functions only; the two
continuous sign certificates establish the all-order bounds.  (c) The old
separated R0.46 large-sector estimate exceeds one at the new target, while
the lattice-sharp tail and the independent canonical-stretch bound pass.  At
the adjacent millionth `r=0.376933`, the lattice tail fails in the exact
`s=162,j=81` column even though the stretch gate remains below one.  (d) The
five disjoint input-charge sectors exhaust the target-radius tail; the fixed
`s=162` sector is limiting, while the infinite `s>=241` sector is bounded by
the even endpoint `s=242,j=121`.

Every threshold decision uses GMP rationals.  The result concerns the reduced
canonical edge generating system and this specific induced two-block
weighted-l1 norm.  It does not prove three-dimensional Navier--Stokes
regularity, finite-time blow-up, or a singularity at the failed probe.
