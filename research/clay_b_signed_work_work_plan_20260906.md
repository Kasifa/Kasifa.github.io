# 真实压力功与时间窗口：当前工作计划

2026-09-06。PressureWorkWindow 合并包的后续计划；
不修改已冻结的 PressureQuotient 包。
原合同 G 的方程、初值类和逐尺度路径保持不变。

## 当前已经区分的三个问题

AH 排除了固定总能量层上的一个压力残差瞬时界，但它的常速平台
不做压力功。AI 用紧支撑化和单泡缩放直接处理真实压力功。
AJ 将真实 NS 的正增长保持在一个统一的重标时间窗内。
AI/AJ 合并为同一个研究小节，不逐条定理各发一版。

本项的目标是精确检验
\[
 H(t)\le H(0)\exp\left[
 C(E_0)\int_0^t(1+\|\nabla u(s)\|_2^2)\,ds\right].
\]
它没有前置系数或加性预算，常数只依赖初始总能量。
不能把对此式的反例扩写成所有 \(L^3\) 估计都不成立。

## 本节冻结门禁

1. AI/AJ 的实际文件独立复核和修改后的完整根任务复核；
   机械检查与科学审查分开记录。
2. 核对最相关的原始文献，区分压力 moderator、Euler 的压力功、
   低黏性短窗和临界空间 norm inflation。相似尺度或措辞不是同一结论；
   检索未发现也不证明新颖性。
3. 只有上述边界清楚、文件和依赖可复现后，才把 AI/AJ 合并冻结，
   由固定发布任务接收一次。本计划不是冻结清单或发布状态。

## 随后回到成熟时间，而非重复初始反例

在单泡尺度 \(r\asymp\epsilon\) 上，AJ 的物理窗口是
\[
 t_\epsilon=\tau_0\epsilon^{5/2},
 \qquad \frac{t_\epsilon}{\epsilon^2}=\tau_0\sqrt\epsilon\longrightarrow0.
\]
真正到达 \(t\ge C\epsilon^2\)，重标变量至少需要
\[
 \tau\ge C\epsilon^{-1/2}.
\]
这超过 AJ 所证的固定 \(\tau_0\) 区间。不能无限拼接局部存在区间、
免费维持高阶范数，也不能把早时初值族向过去平移成成熟历史。

因此本次早时检查结束后，不再只扩大初值振幅或更换临界范数名称。
下一项从 AB 的成熟时间带符号局部恒等式出发：

- 固定 \(r\) 和同一解，明确 \(s\ge Cr^2\) 的时间范围；
- 保留近源压力、外壳输运和黏性项，不将全局早时反例当成局部结论；
- 对每一条新增支付写出时间、空间尺度、初值依赖和可积性来源；
- 只有取得新的定量估计，才尝试接回缩球、原移动路径与合同 G。

若仍只能得到一个额外条件，记录该条件而不宣称能量已经支付。
G、G-P/G-C、成熟持留和首次奇点排除均仍 OPEN。

## 记录与分工

PressureQuotient 已完成研究冻结和单次交接。研究不读取发布队列，
不监督、重试或汇报网页发布；继续自己的数学工作。
AI/AJ 只随整包 manifest 和一次消息移交，不单独补入前序包。
普通小节不更新累计 recap。
本项为纯解析工作，没有仿真或科学图；需要计算时才调用既有
DGX 监控和期刊图档协议。私有论文 v2 不在本项范围内。
