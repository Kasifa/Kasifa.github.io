# R0.74P Chinese reader source — independent audit

## Verdict

**PASS.**  The 392-line Chinese publication source is consistent with the
final mathematical note, certificate, figure contract, terminology lock,
and literature boundary.  No mathematical, quantifier, literature, style,
or HTML-math blocker remains.

Audit bindings:

- Chinese source SHA-256:
  `d1602097fce2ae86089ff5dea678be6d5330366ca867886f51488aabf7c435d4`.
- Mathematical source SHA-256:
  `a3cb872735b92b32ddfa7b96bc4184d70b0287ff2ce7d3da8cadbbcc494d0867`.

## High-risk checks

- The good-time formula for \(\widetilde K_{k,R}\) uses the open interval
  \((s_R,\tau)\times\mathbb T^3\), the moving weight
  \(\Psi_k^R(x-X_R(t))\), and \(d\boldsymbol\mu(t,x)\).
- At all times, (K_{k,R}) is the canonical absolutely continuous
  representative (Q_{k,R}+F_{k,R}); the reader source does not pass a
  spacetime measure through a hard-time atom.
- The smooth exact-family notation has
  \(\alpha\in\{M,F\}\), \(z_M=v_R\), and \(z_F=v_R-a_R\).  General
  suitable-weak and compactness claims are Version M only, at fixed (R)
  and fixed terminal point.
- All windows have positive length.  The optimization uses (xin(0,1)),
  and for \(\sigma>1\) the right-endpoint value is only approached as
  \(x\uparrow1\).
- Exact-family statements retain “all sufficiently large (j)” and
  constants uniform in (j).
- Positive variation and total variation carry \([s_R,t_0)\); the Lin
  topology is local on compact subcylinders; the proposed contraction has
  both \(0<\theta<1\) and \(0<\lambda<1\).
- The figure is described as a decay-rate plot with the unknown additive
  \(\log_{10}C\) suppressed, not an absolute numerical upper curve.
- Only the target component (v_{j,R}) receives a two-sided (T_*) bound;
  the full (Y_{2,R}^{\rm sf}) upper bound remains OPEN.
- Lei--Ren's spatial regular intervals and quantitative regularity epochs,
  and Yu 2026's adjacent moving-window/defect-ledger method, are accurately
  bounded.  The non-hit is not promoted to novelty or priority.

## Rendering and style

- Inline math delimiters: 59 opening and 59 closing.
- Display math delimiters: 32 opening and 32 closing.
- Stack/nesting errors: zero.
- The project MathJax cleanliness check passes with naked-math detection.
- `git diff --check` passes.
- Prose uses the locked first-person singular or neutral formulation.
- No simulation, singularity construction/exclusion, regularity theorem, or
  Clay conclusion is claimed.

**NOT CLAY.**
