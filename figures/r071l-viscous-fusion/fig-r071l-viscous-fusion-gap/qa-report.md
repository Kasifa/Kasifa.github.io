# Visual QA report

Status: passed

The revised figure was regenerated and inspected at 178 mm in color, true
grayscale, and from the vector PDF.
Panel titles, axis labels, legends, scales, units, formulas, hatches, arrows,
and the scope line must remain legible without clipping, and color must not be
required to distinguish the series.

The evidence labels visibly and consistently state: Panel A is exact
algebra; Panel B is a fixed-witness deterministic diagnostic; Panel C is mixed
evidence, with the local heat/support $K^{-4}$ entry labeled as an analytic
upper scale and the fused-tangent $K^{-2}$ entry labeled as a deterministic
diagnostic. The nonzero fused-tangent coefficient must not be presented as an
interval sign certificate. The $O_\nu(K^{-3})$ finite-$K$ remainder must be
attributed to earlier analytic finite-$K$ theory, not to the checker. The
plotted values were cross-checked against `data.csv`, `validation.json`, and
the independent validation archive after rerendering.
