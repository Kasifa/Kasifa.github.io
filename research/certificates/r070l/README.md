# R0.70L exact certificate

This directory locks the exact symbolic payload for the resolved
source-evolution and pressure-blindness gate.

The producer verifies four groups:

1. filtered strain and source/shape matrix algebra;
2. the matched periodic initial-face pair and its complete derivative ledger;
3. the finite-dimensional pressure duality used in the local-functional
   no-go theorem;
4. the two-mode Beltrami pressure/SGS filter split.

Run the command in command.txt from the repository root. The result must be
byte-identical to result.json before SHA verification.

The finite calculation does not computer-prove the continuum
support-separated realization, literature completeness, a weak-solution
passage, regularity, blow-up, or a Millennium-problem solution.
