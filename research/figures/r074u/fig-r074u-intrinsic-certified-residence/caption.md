# Figure caption

**Figure R0.74U-20. Intrinsic motion certifies an `L_i R^3` residence scale
and excludes the exponentially short-dwell escape for the frozen canonical
common-shear lobe.**

**(A)** The defining centre condition
`|Q_i(t)| < A(L_i) r_i`, with `Q_i(tau_i)=0`, is exactly symmetric in centre
displacement. The certified time set is its monotone preimage intersected
with the terminal slab `I_R=(64R^2,65R^2)`. The drawing is an analytic
schematic and is not to scale: slab truncation may remove one side, but at
least one side supplies the uniform lower allowance used in (U.23).

**(B)** The inherited speed interval has scale `Q_i' ~ R^-2`, whereas the
horizontal centre room has scale `A(L_i)r_i=A(L_i)L_iR`. Distance divided by
speed therefore has scale `A(L_i)L_iR^3`. Equations (U.21)--(U.24) make this
kinematic product quantitative and give the two-sided corridor law.

**(C)** The certified geometric corridor satisfies

\[
 {72\over5}L_iR^3\le |\mathscr R_i^{\rm cert}|
 <{1024\over3}L_iR^3,
\]

so it is `Theta(L_iR^3)`. It is contained in the completed-clock
`K_{k_i,R}` superlevel set, which consequently has measure at least
`(72/5)L_iR^3`. Only this lower `Omega(L_iR^3)` statement is proved for the
full superlevel set: the figure asserts neither converse inclusion nor an
upper bound for it.

**(D)** On the explicit analytic path `d_L=log L_1`, `L_2=2L_1`, the proved
certified dwell obeys `theta_cert,2 >= (72/5)L_2`, while the necessary
bounded-payment condition with illustrative `C=1` would require

\[
 \theta_{{\rm cert},2}\le L_2^{1/2}
 \exp[-(5c_\gamma-a_S)L_1^2-d_L],
 \qquad
 5c_\gamma-a_S={603445\over89413632}>0.
\]

The logarithmic gap grows quadratically. A general fixed `C` shifts only the
orange logarithmic upper curve by `log10 C`.

All panels are **ANALYTIC / SCHEMATIC · DERIVED ANALYTIC VALUES · NOT PDE
DATA · NOT DNS · NOT CLAY**. The result is for the frozen canonical
common-shear packet architecture. It is not an arbitrary-packet residence
theorem, not a full-clock upper-residence theorem, and not a regularity or
singularity theorem.
