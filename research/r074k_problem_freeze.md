# R0.74K problem freeze — one dangerous inward collar

## Frozen question

R0.74J proves, on the exact R0.74F--H smooth periodic unforced family,

\[
 P_j:=P_{R_j}^M=P_{R_j}^F\asymp B_j^3R_j^3,
 \qquad
 P_j^{2/3}\sqrt{1+\log_+P_j}\asymp B_j^2L_jR_j^2.
\tag{F.1}
\]

R0.74K does **not** assume either of the still-open matching upper bounds
for \(X_j\) or \(\mathfrak C_j\).  It asks the narrower route-selection
question:

> Which annular packet row prevents the existing Gaussian and bridge
> estimates from proving a matching upper bound, and which direct analytic
> condition would suffice to close the collar-flux
> observable along the selected normalized-bridge route?

The family, annular weights, collar smoothing, amplitudes, and all
Version-M/Version-F conventions are inherited without change from
R0.74F--J.

## Frozen deliverables

1. An exact shell-by-shell exponent comparison for the inward packet tail.
2. A positive-volume check at the nearest inner shell, rather than a
   zero-chord calculation on the shell boundary.
3. An exact sufficient collar-flux statement with every normalization
   displayed.
4. A bounded primary-source comparison with passive-scalar shear-mixing
   results.
5. A formal figure and a finite exact-arithmetic certificate.

## Forbidden extrapolations

This section may not report the new inward-bridge/shear-expulsion estimate
as proved.  It may not turn a familywise implication into a universal
endpoint theorem, infer a good scale at a prescribed possible singular
point, claim novelty or priority, or claim progress sufficient to resolve
the three-dimensional Navier--Stokes Millennium problem.  **NOT CLAY.**
