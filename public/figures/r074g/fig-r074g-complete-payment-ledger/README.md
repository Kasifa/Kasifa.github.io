# R0.74G complete-payment ledger figure

This 24-file journal package records the exact exponent gates and normalized analytic ledger used in the R0.74G rejection of the frozen proposed endpoint inequalities. Panel A is bound to the byte-identical 31/31 finite certificate. Panel B evaluates only the displayed analytic envelopes and lower bound at the exact discrete scales `L_12 = 8064` and `L_13 = 16128`.

The figure is an analytic derivation, not DNS, simulation, or measured data. The finite certificate alone does not prove the heat-kernel, Brownian-bridge, pressure, Peetre, full-payment, or endpoint arguments; those belong to the accompanying R0.74G proof and independent audits. The rejected object is the project’s frozen proposed inequality, not the Navier--Stokes regularity problem. **NOT CLAY.**

Run `plot.py`, inspect all four QA surfaces, then run `validate.py`; see `command.txt`. Both generator and validator refuse source drift in either `scripts/r074g_complete_payment_certificate.py` or `research/r074g_complete_payment_certificate.json`, and independently require the script stdout to be byte-identical to the frozen JSON.
