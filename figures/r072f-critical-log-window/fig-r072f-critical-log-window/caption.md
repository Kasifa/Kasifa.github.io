# Figure R0.72F-1

**Critical-log initial-layer window and three repair vertices.**
**A,** Analytic intersection of the selected-family obstruction and the
Leray energy-payment screen for
`w(s)=s^(-beta)[1+log(1/s)]^gamma`, with `gamma >= 0`. The hatched interior is
`1/3 < beta < 1/2`; the solid endpoint ray is
`beta=1/3, gamma >= 1`. The star marks the minimal power-log endpoint
`(1/3,1)`. The open circle at `(1/3,0)` fails the selected-family logarithmic
test, and the dashed `beta=1/2` line is excluded by the energy-only screen.
This is not a complete-root sufficiency region. **B,** The split-step producer
and independent real-lattice BDF calculations of
`Q_* delta^(2/3)/log(delta)` on the shared dyadic grid. The maximum relative
cross-audit difference is reported in the panel. The plain endpoint
comparison `Q_(1/3,0)/Q_*` decreases from about 0.213 to 0.140, recording the
missing logarithmic payment without placing a differently scaled curve on the
main axis. **C,** Exact frontier vertices at fixed `a=1/3`, after rescaling
the remaining repair budget `beta+c+3 alpha/4=1/3` to barycentric
coordinates. The temporal vertex is `(beta,gamma)=(1/3,1)`, the explicit
coupling vertex is `c=1/3`, and the root-atom vertex is `alpha=4/9`. The last
route changes the left-hand-side observable and is not a right-hand-side
repair of the original ledger. Numerical points are deterministic finite
binary64 diagnostics. They do not supply a complete-root upper bound,
restart control, control of `R_Y`, Navier–Stokes regularity, singularity
formation, or a Millennium-problem solution.
