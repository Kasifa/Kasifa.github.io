**Figure R0.73B — low-gap Bloch weights and physical kinetic transients.**
(A) Deterministic finite `N=10` gains on `[0,0.75]` combine the seven-point
main and seven-point targeted grids into thirteen distinct low-gap values.
Fixed `c=1` kinetic gain and fixed `Lambda=1` raw-`q` gain have the predicted
`mu^{-1/2}` diagnostic scaling, while fixed-`Lambda` physical kinetic gain is
bounded.  (B) At `mu=10^{-8}`, finite fixed-`Lambda` kinetic gains converge to
the explicit triangular low-gap matrix limit and remain below the analytic
energy envelope.  Filled and open markers distinguish the finite and limit
values even when they nearly coincide.  (C) At `d=0`, the elementary
`||W_x||_infinity/2` estimate, the explicit carrier/block upper bound, and the
exact low-gap coefficient `rho_0(0)=||W_x||_2/2=1/4` are analytic curves; no
truncated operator norm is relabelled as exact.  (D) Small-gap exponents for
the four audited diagonal weights agree with the exact block prediction
`(a/2-p)_+` for fixed `c` (`p=0`) and fixed `Lambda` (`p=1/2`).  All numerical
markers are finite Fourier--Galerkin diagnostics without a tail bound; complete
OS/Squire A2 direct sum, nonlinear closure, and the Clay problem remain open.
