# Figure R0.46-1

**Correlated two-block weighted-column certificate.** (a) At the target
radius `r=0.376`, the exact `s=0` endpoint decreases with the charge-zero
weight `kappa`, while the exact weighted `s=-1` endpoint increases; the large
positive-charge bound is independent of `kappa`.  The marked choice
`kappa=3/4` lies in the strict feasible interval and has complete bound
0.997706.  The 101 plotted weights are presentation samples of explicit
rational functions, not the all-order proof.  (b) The correlated two-block
bound passes from `r=0.371` through `0.376`; the old unweighted `s=-1` column
already fails at `0.372`, and the inherited large-sector bound first fails at
the adjacent probe `0.377`.  (c) All five exhaustive target-radius sectors
are shown against threshold one.  The large sector `s>=241` is the active
bottleneck.  (d) The separately maximized `2x2` block matrix and the
unweighted column fail, whereas the correlated tail, ball mapping,
Lipschitz, and canonical-stretch gates pass.  The matrix Perron value is a
display-only square-root evaluation; its exact failure is certified by
`D+BC-1>0`.  Every threshold decision uses GMP rationals.

The theorem concerns the reduced canonical edge generating system.  It does
not prove three-dimensional Navier-Stokes regularity, finite-time blow-up, or
failure of the exact operator at `r=0.377`.
